%h2.page-header Результаты поиска

.row
  .col-md-12{style: 'margin-bottom: 20px'}
    = render 'shared/search_form'

- if @items.empty?
  %h3 Результатов нет

- @items.each do |activity|
  = render 'shared/activity_list_item', activity: activity

= paginate @items
