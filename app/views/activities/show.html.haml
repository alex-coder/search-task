%h2.page-header
  = @item.title
  %small.text-success= @item.city.name

%p= @item.description.html_safe

- @item.tags.each do |tag|
  %a.label.label-default{href: tag_path(tag)}= tag.name

%h3 Достопримечательности
- @item.attractions.each do |attraction|
  = render 'shared/attraction_list_item', attraction: attraction
