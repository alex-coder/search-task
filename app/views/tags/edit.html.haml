%h2.page-header= @item.name

.row
  .col-md-8.col-md-offset-2
    = simple_form_for @item, class: Tag do |f|
      = f.input :name
      = f.input :description, as: :text

      = f.button :submit, "OK"
